Assessment: Java Enums 
1. What is an enum, as defined in the video? 
A. A type of array that can grow in size 
B. A special class that contains a collection of constant values 
C. A method that returns multiple values 
D. A loop used for repeating tasks 
2. Which of the following is a good real-world example of when to use an enum? 
A. Storing a user's age that changes every year 
B. Storing directions like North, East, West, South, which are fixed and won't change 
C. Storing random numbers generated by the program 
D. Storing a list of usernames that keeps growing 
3. What is the conventional naming style for enum constant values in Java? 
A. lowercase 
B. camelCase 
C. ALL CAPS (uppercase) 
D. PascalCase with underscores only 
4. Which keyword is used to declare an enum in Java? 
A. class 
B. enum 
C. interface 
D. struct 
5. What is the correct way to declare a simple enum for difficulty levels? 
A. class AI_Level { EASY, MEDIUM, HARD } 
B. enum AI_Level { EASY, MEDIUM, HARD } 
C. interface AI_Level(EASY, MEDIUM, HARD) 
D. enum AI_Level = [EASY, MEDIUM, HARD] 
6. According to the video, what is the best practice for organizing an enum in a project? 
A. Always declare it inside the main class only 
B. Give the enum its own separate file, rather than declaring it inside another class 
C. Never use enums in real projects 
D. Enums must be declared inside a loop 
7. How do you access a specific value from an enum (e.g., getting HARD from AI_Level)? 
A. AI_Level.get(HARD) 
B. AI_Level->HARD 
C. AI_Level.HARD 
D. new AI_Level(HARD) 
8. When comparing enum values inside an if statement, which comparison operator can be used? 
A. Only < and > 
B. Only <= and >= 
C. Only == (equality) 
D. Any operator works the same way 
9. In a switch statement using an enum, do you need to write the enum name before each case value (e.g., case 
AI_Level.EASY)? 
A. Yes, it is always required 
B. No, since the switch already knows the type, you can just write the value name (e.g., case EASY) 
C. Only for the first case 
D. Only if the enum has more than 3 values 
10. Why are enums especially useful in decision-making structures like if-else and switch statements? 
A. They make the program compile faster 
B. They constrain values to a fixed, predefined set, avoiding errors from typos or invalid inputs (unlike plain 
strings) 
C. They automatically sort data 
D. They eliminate the need for variables 
11. What makes an enum different from a normal class, based on the video? 
A. Enums cannot have any methods 
B. Enums are still a type of class/object and can hold their own values through a constructor, but represent a 
fixed set of constants 
C. Enums can never be used in loops 
D. Enums must always be public 
12. In the US Currency example, what does adding a constructor to the enum allow you to do? 
A. Create unlimited new coin types at runtime 
B. Give each enum constant (like PENNY, NICKEL) its own associated value, such as 0.01 or 0.05 
C. Remove the need for the enum keyword 
D. Automatically convert the enum into an array 
13. Why did the instructor warn against making the enum's value field private without adding getters? 
A. Private fields cause the program to crash 
B. Making it private without a getter would prevent the main method from accessing that value directly 
C. Private fields are not allowed in enums 
D. It would delete the enum automatically 
14. In the video's example, what was the total sum when adding the values of a dime, nickel, and quarter? 
A. 0.30 
B. 0.35 
C. 0.40 
D. 0.45 
15. Can you create an array of enum type, such as `USCurrency[] coins`? 
A. No, enums cannot be placed inside arrays 
B. Yes, an array of enum values can be created just like other object arrays 
C. Only if the enum has no constructor 
D. Only for enums with exactly two values 
16. What Java feature did the instructor use to add both a name and a value to each enum constant 
(constructor overloading)? 
A. Creating two constructors with different parameters (one with just value, one with name and value) 
B. Declaring the enum twice 
C. Using two separate enums for the same data 
D. Making all fields static 
17. What loop style did the instructor use to go through the array of enum coins and calculate their total 
value? 
A. A while loop only 
B. A for-each loop 
C. A do-while loop 
D. Recursion 
18. According to the video, what happens if you try to use less-than (<) or greater-than (>) to compare enum 
values? 
A. It works exactly the same as == 
B. This is not how enums should be compared — only pure equality (==) is applicable for enum comparisons 
C. It automatically converts the enum into a number 
D. It causes the program to run infinitely 
19. What real-world scenario did the instructor use to explain why enums prevent invalid values (compared 
to plain strings/characters)? 
A. Student grades that can be any letter 
B. Philippine peso coins/bills, which only exist in specific denominations (no such thing as a 6-peso coin) 
C. Random number generation 
D. File naming conventions 
20. What is the overall purpose of enums, as summarized in the video? 
A. To replace all classes in a Java program 
B. To represent a fixed, predefined set of constant values (optionally with their own associated data), making 
code safer and clearer especially in decision-making 
C. To make loops run without conditions 
D. To store data that changes frequently during runtime 

Answer Key 

1 B
2 B
3 C
4 B
5 B
6 B
7 C
8 C
9 B
10 B
11 B
12 B
13 B
14 C
15 B
16 A
17 B
18 B
19 B
20 B
